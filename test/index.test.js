/* --------------------
 * @overlook/router-match module
 * Tests
 * ------------------*/

'use strict';

// Modules
const routerMatch = require('../index');

// Init
require('./utils');

// Tests

describe('tests', () => {
	it.skip('all', () => { // eslint-disable-line jest/no-disabled-tests
		expect(routerMatch).not.toBeUndefined();
	});
});
